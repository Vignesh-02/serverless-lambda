

service: serverless-lambda

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-1


functions:
  function_test1:
    handler: src/handler.generateRandomNumber
    timeout: 60
    memorySize: 128
